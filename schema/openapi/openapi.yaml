openapi: '3.0.3'
info:
  title: Backbone API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    This API provides 11 standard CRUD endpoints for each model.

servers:
  - url: /
    description: Development server

tags:
  - name: AuditTrails
    description: AuditTrail management endpoints

paths:
  /api/v1/auditlog/audit_trails:
    get:
      tags:
        - AuditTrails
      summary: List AuditTrails
      description: Get a paginated list of AuditTrails with optional filtering and sorting
      operationId: listAuditTrails
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrailList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - AuditTrails
      summary: Create AuditTrail
      description: Create a new AuditTrail
      operationId: createAuditTrail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAuditTrailRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/auditlog/audit_trails/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - AuditTrails
      summary: Get AuditTrail by ID
      operationId: getAuditTrail
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - AuditTrails
      summary: Update AuditTrail (full)
      operationId: updateAuditTrail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuditTrailRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - AuditTrails
      summary: Update AuditTrail (partial)
      operationId: patchAuditTrail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchAuditTrailRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - AuditTrails
      summary: Delete AuditTrail (soft)
      operationId: deleteAuditTrail
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/auditlog/audit_trails/bulk:
    post:
      tags:
        - AuditTrails
      summary: Bulk create AuditTrails
      operationId: bulkCreateAuditTrails
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateAuditTrailRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AuditTrail'
    put:
      tags:
        - AuditTrails
      summary: Bulk update AuditTrails (full, atomic)
      operationId: bulkUpdateAuditTrails
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateAuditTrailRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrailBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - AuditTrails
      summary: Bulk partial-update AuditTrails (atomic)
      operationId: bulkPatchAuditTrails
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrailBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/auditlog/audit_trails/upsert:
    post:
      tags:
        - AuditTrails
      summary: Upsert AuditTrail
      operationId: upsertAuditTrail
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuditTrailRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'

  /api/v1/auditlog/audit_trails/trash:
    get:
      tags:
        - AuditTrails
      summary: List deleted AuditTrails
      operationId: listDeletedAuditTrails
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrailList'

  /api/v1/auditlog/audit_trails/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - AuditTrails
      summary: Restore deleted AuditTrail
      operationId: restoreAuditTrail
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrail'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/auditlog/audit_trails/empty:
    delete:
      tags:
        - AuditTrails
      summary: Empty trash for AuditTrails
      operationId: emptyTrashAuditTrails
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/auditlog/audit_trails/delete/bulk:
    post:
      tags:
        - AuditTrails
      summary: Bulk soft-delete AuditTrails by ids (atomic)
      operationId: bulkDeleteAuditTrails
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/auditlog/audit_trails/restore/bulk:
    post:
      tags:
        - AuditTrails
      summary: Bulk restore AuditTrails by ids (atomic)
      operationId: bulkRestoreAuditTrails
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditTrailBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/auditlog/audit_trails/restore/all:
    post:
      tags:
        - AuditTrails
      summary: Restore all soft-deleted AuditTrails
      operationId: restoreAllAuditTrails
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/auditlog/audit_trails/trash/bulk:
    delete:
      tags:
        - AuditTrails
      summary: Bulk permanently-delete AuditTrails from trash by ids (atomic)
      operationId: bulkPermanentDeleteAuditTrails
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

components:
  schemas:
    AuditTrail:
      type: object
      required:
        - id
        - occurred_at
        - event_type
        - action
        - actor
        - status
        - txid
      properties:
        id:
          type: string
          format: uuid
        occurred_at:
          type: string
          format: date-time
        event_type:
          type: string
        action:
          type: string
          maxLength: 120
        actor:
          type: string
          maxLength: 160
        subject_type:
          nullable: true
          type: string
          maxLength: 120
        subject_id:
          nullable: true
          type: string
          maxLength: 160
        changed:
          nullable: true
          type: object
        reason:
          nullable: true
          type: string
        status:
          type: string
        correlation_id:
          nullable: true
          type: string
          maxLength: 64
        client_ip:
          nullable: true
          type: string
          maxLength: 64
        user_agent:
          nullable: true
          type: string
          maxLength: 512
        http_method:
          nullable: true
          type: string
          maxLength: 10
        resource_path:
          nullable: true
          type: string
          maxLength: 512
        txid:
          type: string
          maxLength: 32

    AuditTrailBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditTrail'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateAuditTrailRequest:
      type: object
      required:
        - occurred_at
        - event_type
        - action
        - actor
        - status
        - txid
      properties:
        occurred_at:
          type: string
          format: date-time
        event_type:
          type: string
        action:
          type: string
          maxLength: 120
        actor:
          type: string
          maxLength: 160
        subject_type:
          nullable: true
          type: string
          maxLength: 120
        subject_id:
          nullable: true
          type: string
          maxLength: 160
        changed:
          nullable: true
          type: object
        reason:
          nullable: true
          type: string
        status:
          type: string
        correlation_id:
          nullable: true
          type: string
          maxLength: 64
        client_ip:
          nullable: true
          type: string
          maxLength: 64
        user_agent:
          nullable: true
          type: string
          maxLength: 512
        http_method:
          nullable: true
          type: string
          maxLength: 10
        resource_path:
          nullable: true
          type: string
          maxLength: 512
        txid:
          type: string
          maxLength: 32

    UpdateAuditTrailRequest:
      type: object
      required:
        - occurred_at
        - event_type
        - action
        - actor
        - status
        - txid
      properties:
        occurred_at:
          type: string
          format: date-time
        event_type:
          type: string
        action:
          type: string
          maxLength: 120
        actor:
          type: string
          maxLength: 160
        subject_type:
          nullable: true
          type: string
          maxLength: 120
        subject_id:
          nullable: true
          type: string
          maxLength: 160
        changed:
          nullable: true
          type: object
        reason:
          nullable: true
          type: string
        status:
          type: string
        correlation_id:
          nullable: true
          type: string
          maxLength: 64
        client_ip:
          nullable: true
          type: string
          maxLength: 64
        user_agent:
          nullable: true
          type: string
          maxLength: 512
        http_method:
          nullable: true
          type: string
          maxLength: 10
        resource_path:
          nullable: true
          type: string
          maxLength: 512
        txid:
          type: string
          maxLength: 32

    PatchAuditTrailRequest:
      type: object
      properties:
        occurred_at:
          type: string
          format: date-time
        event_type:
          type: string
        action:
          type: string
          maxLength: 120
        actor:
          type: string
          maxLength: 160
        subject_type:
          nullable: true
          type: string
          maxLength: 120
        subject_id:
          nullable: true
          type: string
          maxLength: 160
        changed:
          nullable: true
          type: object
        reason:
          nullable: true
          type: string
        status:
          type: string
        correlation_id:
          nullable: true
          type: string
          maxLength: 64
        client_ip:
          nullable: true
          type: string
          maxLength: 64
        user_agent:
          nullable: true
          type: string
          maxLength: 512
        http_method:
          nullable: true
          type: string
          maxLength: 10
        resource_path:
          nullable: true
          type: string
          maxLength: 512
        txid:
          type: string
          maxLength: 32

    AuditTrailList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditTrail'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  requestBodies:
    BatchIds:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [ids]
            properties:
              ids:
                type: array
                items: { type: string }

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
